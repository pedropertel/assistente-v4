# Dev Log — 2026-07

## 2026-07-06 — Retomada após ~2 meses + reconciliação REGRA 11 + pausa oficial da 3.F

### Contexto

Projeto ficou parado desde início de maio. Motivo real da parada:
a integração Meta (3.F) travou num bloqueio externo — o Meta
Business do CEDTEC está em nome da esposa do Pedro; Pedro criou
a página, mas a autenticação como dono da empresa não funcionou.
A retomada da 3.F vai exigir fazer o fluxo de token/ad_account
pela conta dela. Decisão do Pedro em 2026-07-06: **pausar a 3.F
e avançar por outra frente**, voltando ao Meta quando o acesso
existir.

### Reconciliação REGRA 11 (fechamento retroativo da 3.F.0.5)

STATUS.md estava atrás do git — sinal clássico de `/fechar`
esquecido na última sessão de maio:

- STATUS dizia "3.F.0.5 ⏳ aguardando OK" mas o commit
  `9730fc2 feat(3.F.0.5)` já estava feito e pushed (docs
  `Tabela — chat_mensagens.md` +93 e `CONVENÇÕES.md` +32,
  documentando `tool_calls`/`tool_results jsonb`).
- A atualização do STATUS de 2026-05-03 (plan 3.F oficializado)
  nunca foi commitada — ficou 2 meses como working tree sujo.
- Dev Log de maio não tinha entrada da 3.F.0.5.

Reconciliação aplicada: STATUS.md atualizado (3.F.0.5 ✅,
3.F ⏸️ pausada, histórico corrigido) + esta entrada retroativa.

### Achado operacional: Supabase INACTIVE

O projeto `msbwplsknncnxwsalumd` foi **pausado pelo free tier
por inatividade** — status INACTIVE confirmado via API em
2026-07-06. Consequências:

- Produção (assistente-v4.vercel.app) está fora do ar: sem
  auth, sem banco, sem Edge Functions até restore.
- Pendência técnica: confirmar no banco se o ALTER TABLE da
  3.F.0.5 (`tool_calls`/`tool_results` em `chat_mensagens`)
  foi de fato aplicado — o commit `9730fc2` só toca docs, e a
  verificação SQL ficou impossível com o projeto pausado.
- Restore aguardando OK do Pedro (ação em infra compartilhada).

### Resolução (mesma sessão, 2026-07-06)

1. **Restore executado com OK do Pedro.** ~3min de COMING_UP.
   Gotcha registrado: durante o restore o Postgres fica
   acessível com schema `public` VAZIO (backup ainda carregando)
   — parece perda de dados mas não é. Nunca escrever no banco
   antes do status voltar a ACTIVE/200 no REST.
2. **Dados íntegros pós-restore:** 19 tabelas public, 1 usuário,
   1 secret Vault, 29 mensagens, 6 personas, Edge Functions
   intactas (chat-claude v42 ACTIVE).
3. **ALTER da 3.F.0.5 confirmado aplicado** — `tool_calls` +
   `tool_results` existem em `chat_mensagens`. A suspeita de
   "commit só de docs" era falso alarme (a checagem inicial
   rodou no banco mid-restore).
4. **Pedro escolheu 3.I (Marina + tools) como próxima frente**
   — constrói o loop genérico de function calling que a 3.F
   vai reusar quando o Meta destravar.

### Pendências abertas

1. Futuro, sem data: retomar 3.F via conta da esposa
   (token + ad_account do Meta Business).
2. Free tier pausa o projeto de novo após ~7 dias sem uso —
   considerar ping periódico ou upgrade quando o sistema
   entrar em uso diário.

## 2026-07-06 — 3.I ✅ Marina + tools (loop genérico de function calling)

### O que foi feito

Primeira sub-fase com function calling real. 3 commits + 3 deploys
na mesma sessão da retomada:

| Commit | Deploy | Conteúdo |
|---|---|---|
| `ea713f0` (3.I.1) | v43 | Loop genérico: `ToolDef`/`ToolContext`, execução `tool_use`→`tool_result` (máx 3 voltas), métricas acumuladas, gravação em `tool_calls`/`tool_results` (inclusive no caminho de erro) |
| `6c3b9ec` (3.I.2) | v44 | Tool `salvar_ideia`: INSERT em `ideias` com `origem='chat'`, `status='capturada'`, rastreio `mensagem_origem_id`, tags sanitizadas. Modelo controla só conteúdo; Edge controla classificação |
| `ec7bae5` (3.I.2.1) | v45 | Tools transversais: `TOOLS_TRANSVERSAIS` disponíveis em todo turn (inclusive sem persona); `TOOLS_POR_PERSONA` vira exceção pra tools com credencial/risco (Meta na 3.F) |

- Deploys via MCP Supabase (CLI local sem login; Pedro autorizou
  cada deploy).

### Decisão de filosofia (Pedro, 2026-07-06)

**Tools são capacidades do SISTEMA, não da persona. Persona define
o TOM, não o PODER.** Motivo: tool presa a persona cria o bug de
UX mais enganoso possível — persona sem a tool responde "Anotado ✓"
sem gravar nada (validado 3× nos testes). Exceção mantida: tools
com credenciais/writes de risco (Meta) continuam gated por persona.
Na 3.G, registro de tools migra pra `configuracoes`.

### Fix no prompt do Roteador (UPDATE com OK do Pedro)

O seed da 3.D tinha 3 defeitos, todos validados em teste real:

1. Enum de saída `"marcos|bruno|marcela|alemao|null"` — **Marina
   não era opção válida**; nunca seria escolhida (nem com a lista
   dinâmica B14, que informa existência mas não regra de uso).
2. Sem regra de captura de ideia → "anota essa ideia sobre a
   CEDTEC" ia pro Marcos (que fingia salvar).
3. `null` entre aspas ensinava o modelo a devolver a STRING
   "null" — origem dos warnings `persona_invalida` desde a 3.D.

Correção: enum com marina, regra 0 (captura de ideia = prioridade
máxima, mesmo mencionando empresa), null literal JSON. Novo
contexto: 2.675 chars (era 9.350 — o tamanho antigo incluía
padding de espaços de um paste antigo).

### Validação fim-a-fim

- "guarda essa ideia... CEDTEC" → marina → `salvar_ideia` →
  row em `ideias` com título, 5 tags, `proxima_acao_sugerida`,
  rastreio. `tool_calls`/`tool_results` gravados na mensagem.
- Duplicata: Marina reconheceu ideia repetida via histórico e
  NÃO chamou a tool (comportamento desejável, de graça).
- Custo do fluxo com tool: ~R$ 0.15-0.16 (Sonnet, 2 voltas).
  Sem tool: inalterado (~R$ 0.01-0.04).

### Limitações conhecidas (não bloqueiam)

1. **Mensagem mista** (pergunta pro Marcos + "anota essa ideia")
   → Roteador prioriza captura, ideia é salva mas a pergunta fica
   sem resposta. Contorno: mandar em 2 mensagens. Melhoria futura:
   Roteador multi-intenção (Fase 5).
2. **Personas ainda "fingem" ações que não têm tool** (ex: Alemão
   "anotando" lançamento do sítio antes da 3.H). Mitigado pra
   ideias (transversal); morre de vez conforme 3.F/3.H entregam
   as tools que faltam.
3. Cache de isolate segura prompt velho do Roteador por até ~5min
   após UPDATE em `personas` (gotcha conhecido desde 3.C).

### Pendências

1. Pedro testa no celular ("guarda essa ideia: ...") → "aprovado"
   → `/aprovar` (merge dev→main).
2. 3.G ganhou item: migrar registro de tools pra `configuracoes`
   (junto com MAPA/PRICING).
3. Divergência cosmética: v44 foi deployada com 1 linha de
   comentário a mais que o commit `6c3b9ec` (nota de header);
   reconciliada no commit da 3.I.2.1.

→ **Promovida pra produção em 2026-07-06** (merge `bd7bf49`,
dev→main com 8 commits: 3.F.0.5 retroativa + docs da retomada +
hook SessionStart + 3.I completa). Teste do Pedro validado:
ideia "Montar um simulador de poker" salva via chat com chip
Marina.

## 2026-07-06 — 3.E ✅ Streaming SSE token-a-token

### Análise prévia (3.E.0 — pedida pelo Pedro)

Achados que mudaram o desenho:

1. **Streamar só o texto resolvia metade da espera** — o Roteador
   come 1.2-2.2s não-streamáveis no início. Solução: evento SSE
   `router` emitido assim que a persona é decidida → front mostra
   chip + "digitando…" ~2s antes do 1º token.
2. **EventSource não faz POST** e `supabase.functions.invoke` não
   expõe stream → novo helper `invokeFunctionStream` (fetch +
   ReadableStream + parser SSE mínimo) em `core/supabase.js`.
3. **Erro pós-primeiro-byte** não tem mais status HTTP → vira
   evento `error` com o mesmo shape do JSON de erro.
4. **Bônus aprovado:** histórico buscado em PARALELO com o
   Roteador (era em série) — ~300-500ms a menos POR mensagem,
   nos dois modos.

### Entregue

| Commit | O quê |
|---|---|
| `47f6f11` (3.E.1, Edge v46) | Flag `stream: true` opt-in. Pipeline fatorado e compartilhado (JSON = comportamento v45 idêntico). Eventos `router`/`delta`/`tool`/`done`/`error`. `client.messages.stream()` + `finalMessage()` por volta do loop de tools (resto do loop inalterado). `corpoErro()` fatorado pros 2 modos. Desconexão do cliente não aborta INSERTs. |
| `abcd6fc` (3.E.2, front) | `invokeFunctionStream` em `core/supabase.js` (auth igual ao invoke: session token + apikey). `enviarMensagem` streamando: bolha criada no evento `router` com chip da persona, deltas appendam texto, `tool` mostra "⚙️ executando ação…", `done` → `carregarHistorico()` reconcilia com métricas do banco. Sem mudança de HTML/window bridge. |

### Validação

- curl modo JSON: idêntico à v45 (e `persona_escolhida: null` agora
  é null literal — fix do Roteador da 3.I confirmado).
- curl modo SSE: `router` → `delta`s → `done` com payload completo.
- Pedro no preview (3.E.3): "funcionou, ficou rápido e mostra
  parcial". Latência percebida: chip em ~1.5s vs 4-13s de tela
  parada antes.

### Rollout seguro

Produção (main) segue com front sem stream até o aprovado — a Edge
v46 serve os dois modos simultaneamente. Rollback do streaming =
remover `stream: true` do front (1 linha).

### Pendências/futuro

1. Markdown na resposta renderiza como texto puro (`**negrito**`
   aparece literal) — comportamento pré-existente, candidato pra
   Fase 4 (UI do chat).
2. 3.G: migrar tools/mapeamentos pra `configuracoes` (já anotado).

→ **Promovida pra produção em 2026-07-06** (merge `b2bb72b`,
dev→main). Streaming ativo no PWA de produção.

## 2026-07-06 — 3.G.1 ✅ Cotação USD→BRL real (3.G em andamento)

- `_shared/cotacao.ts` (`87c98dd` + fix `73789af`): cache 1h por
  isolate, dedup in-flight, timeout 2s por fonte, sanidade 1-20,
  fallback cache velho → 5.0. Aquecida em paralelo no pipeline.
- **Achado REGRA 11:** awesomeapi (decisão da 3.B em CONVENÇÕES)
  devolve `429 QuotaExceeded` pro IP de egress compartilhado do
  Supabase — validado com função `debug-cotacao` dentro do runtime
  (depois neutralizada pra stub 410; deletável via Dashboard).
  Fontes reais: **er-api → currency-api CDN** (ambas 200/~60ms,
  atualização ~diária — suficiente pra exibição de custo).
- Validado na v48: cotação implícita 5.1784 = er-api 5.178387
  (antes: fixa 5.0, ~3,5% de erro no custo exibido).
- Pendentes da 3.G: 3.G.2 (configs em `configuracoes`) e
  3.G.3 (rate limit).
